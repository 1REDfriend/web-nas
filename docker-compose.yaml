services:
  webnas:
    build:
      context: ./
      dockerfile: Dockerfile
    volumes:
      - ./:/app
      - /app/node_modules
      - /:/host_root
    env_file:
      - ./.env
    environment:
      - NODE_ENV=development
    networks:
      - nas-network
    restart: unless-stopped

  webssh:
    build: ./webssh
    networks:
      - nas-network
    restart: unless-stopped

  caddy:
    image: caddy:alpine
    restart: unless-stopped
    ports:
      - "5492:8888"
      - "5491:5491"
    volumes:
      - ./CaddyFile:/etc/caddy/Caddyfile
    networks:
      - nas-network

networks:
  nas-network:
    driver: bridge
